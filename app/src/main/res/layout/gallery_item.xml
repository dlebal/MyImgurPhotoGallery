<?xml version="1.0" encoding="utf-8"?>

<!-- Wrapper -->
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/gallery_item_llWrapper"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <!-- Photo -->
    <ImageView
        android:id="@+id/gallery_item_ivPhoto"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_margin="@dimen/gallery_item_ivPhoto_layout_margin"
        android:adjustViewBounds="true"
        android:contentDescription="@string/gallery_item_ivPhoto_contentDescription"
        android:scaleType="centerCrop"
        app:layout_constraintDimensionRatio="H,1:1"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- Delete photo -->
    <Button
        android:id="@+id/gallery_item_bDeletePhoto"
        android:layout_width="@dimen/gallery_item_bDeletePhoto_layout_width"
        android:layout_height="@dimen/gallery_item_bDeletePhoto_layout_height"
        android:layout_margin="@dimen/gallery_item_bDeletePhoto_layout_margin"
        android:background="@android:drawable/ic_menu_delete"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</android.support.constraint.ConstraintLayout>
